services:
  app:
    image: aitodo:latest
    restart: unless-stopped
    ports:
      - "6001:3000"
    env_file: .env
    networks:
      - default
      - 1panel-network

networks:
  1panel-network:
    external: true
